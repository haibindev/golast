interface:
  display_name: "GoLast"
  short_description: "Mentioning it alone should recover the previous workspace session"
  default_prompt: "Use $golast to recover the previous workspace session's actionable request and continue from it."

policy:
  allow_implicit_invocation: true
